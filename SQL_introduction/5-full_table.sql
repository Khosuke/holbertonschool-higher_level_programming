-- Displays the exact SQL statement needed to recreate the table named 'first_table'
-- including all of its column definitions, indexes, constraints, and other properties.
SHOW CREATE TABLE first_table;
